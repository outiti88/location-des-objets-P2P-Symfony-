{% extends 'base.html.twig' %}
{% block title %}
  Accueil
{% endblock %}
{% block body %}
        <div class="slider_area">
        <div class="single_slider  d-flex align-items-center slider_bg_1 overlay2">
            <div class="container">
                <div class="row">
                    <div class="col-xl-12">
                        <div class="slider_text text-center ">
                            <h3 class="t1">Location des Objets </h3>
                            <h4 class="t1" style="color:white;">Partout au MAROC </h6>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

<div style="padding:20px;">
    <h2 style="text-align:center; color: firebrick;"><i class="fas fa-medal"></i> LES OBJETS PREMIUM</h2>
    <div class="row mt-5">
      {% for data in ads %}
        {% include 'ad/_ad.html.twig' with {'ad': data.annonce} %}
      {% endfor %}
    </div>
  </div>
</div>
{% endblock %}